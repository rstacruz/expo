// Sample task
/*

module.exports = function(app) {
  app.cli()
    .command('db-seed')
    .description('Pre-fills the database with data')
    .action(function() {

      app.load();
      // Do things here

    });
};

*/
